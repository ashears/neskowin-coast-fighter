# Neskowin Coast Fighter

A browser fighting game built with Phaser, TypeScript, and Vite.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

The GitHub Pages workflow builds the app and deploys the `dist` directory on pushes to `main`.
